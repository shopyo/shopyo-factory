#!/usr/bin/env python
from setuptools import setup

if __name__ == "__main__":
    setup(install_requires=["shopyo @ git+https://github.com/shopyo/shopyo@dev#egg=shopyo"])
    #setup(install_requires=["shopyo==4.1.2"])
